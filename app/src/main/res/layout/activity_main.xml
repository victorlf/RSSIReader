<!--activity_main.xml includes a ListView which will contain details of devices found and a Button which
we’ll use to trigger the scanning process. The ListView needs some XML which defines the structure of
each member of the list as well, so we’ll add that next.-->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="10dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="10dp"
    android:orientation="vertical">
    <!--tools:context=".ui.PeripheralControlActivity"-->

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="left"
        android:paddingLeft="10dip"
        android:paddingBottom="10dp"
        android:text="Bluetooth LE RSSI Reader"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="20dp" />
    <ListView
        android:id="@+id/deviceList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:paddingLeft="20dip"
        android:layout_weight="1" />
    <Button
        android:id="@+id/scanButton"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:enabled="true"
        android:onClick="onScan"
        android:text="" />

    <Button
        android:id="@+id/localButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="true"
        android:gravity="center"
        android:onClick="goToLocation"
        android:text="Location" />
</LinearLayout>